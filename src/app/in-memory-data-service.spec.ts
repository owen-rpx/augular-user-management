import { InMemoryDataService } from './in-memory-data-service';

describe('InMemoryDataService', () => {
  it('should create an instance', () => {
    expect(new InMemoryDataService()).toBeTruthy();
  });
});
